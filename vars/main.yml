---
# vars file for ansible-role-vault

consul_download_url: "https://releases.hashicorp.com/consul/{{ consul_version }}/consul_{{ consul_version }}_linux_amd64.zip"

# Do not set this here, this is a sane default used only in case autogeneration fails
consul_encrypt_key: "Luj2FZWwlt8475wD1WtwUQ=="
